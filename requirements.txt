# Phase 1 -- repo, config, logging. Later phases will extend this file;
# nothing here should be removed without checking which phase depends on it.

pyyaml>=6.0
python-dotenv>=1.0

# Testing
pytest>=8.0
pytest-cov>=5.0

# Phase 2 -- data layer. kiteconnect is only required for
# ZerodhaKiteProvider (src/data/providers/zerodha_kite.py); the historical
# CSV provider and all integrity checks run without it, so it is optional
# until real broker credentials are wired up.
# kiteconnect>=5.0
